# PPTX 본문 추출 무결성 — 2026-09-25

회사 PC의 약 321MB PPTX 읽기 성공 보고 후 실제 추출 경로를 검토했다. 기존 extractPptxYauzl은 압축파일 전체/미디어를 메모리로 읽는 대신 슬라이드와 노트 XML만 읽는다. 따라서 파일 크기만으로 추출 불가를 단정하지 않는다. 2026-09-26 사용자 전달 보고에서 실제 사용자 샘플 1건의 추출·청킹 성공이 확인됐다. 아래 실기 결과 참고.

## 재현한 결함

- 첫 슬라이드 성공 후 다음 XML의 openReadStream 콜백이 실패하면 오류를 버리고 진행해 부분 본문이 status=ok가 됐다.
- ZIP 자체가 오류를 내고 열려 있는 스트림이 끝나지 않으면 pending이 줄지 않아 완료되지 않을 수 있었다.
- 스트림 오류 후에도 다음 항목을 읽었고 즉시 자원을 정리하지 않았다.

## 수정과 검증

XML 열기/읽기 또는 ZIP 오류는 즉시 전체 추출 실패로 처리한다. 활성 스트림과 ZIP을 닫고, 뒤늦은 콜백은 결과를 바꾸지 못한다. 추출 시작 전에 이벤트 수신기를 등록한다. 정상 파일은 기존처럼 슬라이드/노트 본문만 반환한다.

tests/luna/nas-pptx-extraction.test.cjs는 네 종류의 오류를 주입해 부분 본문이 정상으로 반환되지 않는지, 자원이 정리되는지 검증한다. 별도 실제 ZIP 파일로 슬라이드/노트가 추출되고 미디어는 열지 않는지 확인한다. 수정 전에는 오류 재현 4건이 실패/시간 초과했고 수정 후 5건 모두 통과했다.

## 범위와 남은 항목

운영 DB, 회사 PC 파일과 작업 스케줄러는 변경하지 않는다. 과거에 부분 추출된 파일을 식별하거나 자동 재색인하지 않는다. 다른 실제 파일의 파싱 시간/메모리, XML 텍스트 해석 완전성, 청크 교체의 원자성은 별도 검증/개선 대상이다. 기존 실패 파일의 재처리 누락과 부분 실패의 완료 오기록 수정은 nas-text-retry-integrity.md에 기록한다. 이번 수정은 과거 DB 자료가 완전하다는 증거가 아니다.

## 회사 PC 실기 — 2026-09-26 11:13 KST 사용자 전달 보고

이 시각은 보고 수신 시각이며 실제 실행 시각은 별도 제공되지 않았다. ChatGPT가 NAS를 직접 읽은 결과가 아니다.

| 항목 | 결과 |
|---|---|
| 실행 | TJLEE / tjlee\tjlee / Console ID 1, SSH 아님 |
| 검증 코드 | 2df7eae32a7cff03b837d7a689d6e4464e8018c9 |
| 대상 | 앞서 1바이트 읽기에 성공한 동일 PPTX 1건 |
| 파일 크기 | 320,750,445바이트 |
| 추출 | ok, 3,665자 |
| 청킹 | 5개, 잘림 없음 |
| 시간 | 추출·청킹 61ms, 프로세스 전체 747ms |
| 종료 | 0, 제한 120초 이내 |
| 파일 변경 | 검사 전후 크기·수정 시각 동일 |

extractNasFileText와 chunkNasText만 호출했으며, index-nas-text.ts·DB 쓰기·임베딩·전체 스캔·스케줄러 변경·배포는 실행하지 않았다는 보고다. 기존 저장소와 미커밋 변경도 보존됐다.

이는 해당 파일의 추출기 완료와 분할 결과를 검증한다. 사람이 슬라이드 내용과 대조한 의미적 완전성, 이미지 속 텍스트 OCR, DB 저장, 벡터 검색까지 검증한 것은 아니다. PPTX는 필요한 XML만 읽으므로 321MB 전체를 61ms에 처리했다는 처리량 주장으로 사용하지 않는다. 이 샘플의 같은 검사를 반복하지 않는다.
