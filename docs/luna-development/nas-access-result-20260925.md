# NAS 접근 검사 결과와 판정 범위 — 2026-09-25

## 최신 판정 — 21:14 KST 사용자 전달 보고

아래는 사용자가 전달한 회사 PC Cursor의 결과이며, ChatGPT 환경에서 NAS를 직접 읽은 결과는 아니다. **현재 로컬 콘솔 세션의 루트 및 문서·이미지 최소 읽기는 통과했다.** 앞선 SSH 실패를 NAS 전체 장애나 파일 부재로 취급하지 않는다.

| 항목 | 확인 결과 |
|---|---|
| 실행 | TJLEE / tjlee\\tjlee / D:\\Dev\\apollon-nas-scanner |
| 세션 | SESSIONNAME=Console, ID 1, SSH_CLIENT/SSH_CONNECTION/SSH_TTY 없음 |
| work | T:\\ = \\\\aiw\\work, 두 경로 읽기 성공 |
| partners | P:\\ = \\\\aiw\\partners, 두 경로 읽기 성공 |
| 잘못된 후보 | T:\\work는 ENOENT. 이 경로를 설정에 넣지 않는다 |
| 문서 | T의 실제 PPTX, 320,750,445바이트, 첫 1바이트 읽기 성공 |
| 이미지 | P의 실제 JPG, 17,213,051바이트, 첫 1바이트 읽기 성공 |
| 스크립트 | 초기 probe의 CRLF SHA-256 d6e1438de13c736fe4372edd5b061c46c104b1e7eee1133f7111b618ff6fc74b |
| 종료 코드 | 1. 존재하지 않는 추가 후보 T:\\work 때문이며 유효 루트/샘플의 실패가 아님 |

net use와 최상위 이름 집합 비교도 T/UNC 및 P/UNC 대응을 뒷받침한다. Y/Z는 검증 대상이 아니다. T/P는 현재 net use에서 OK지만 HKCU\\Network에는 Y/Z만 확인됐으므로 재로그인·재부팅 후의 지속성을 확정하지 않는다. 인증 창은 뜨지 않았으며 저장소, 매핑, 설정, DB, 스케줄러는 변경하지 않았다는 보고다.

그 직전 SSH 조사에서는 같은 TJLEE, 같은 계정, NAS TCP 445 성공, T/P 부재와 Y/Z Unavailable이 확인됐다. OpenSSH 로그의 publickey 기록만으로 해당 Cursor 연결의 인증 방식을 확정할 수 없다. **세션별 접근 차이는 확인됐지만 정확한 인증 원인은 미확정**이다. 현재 성공을 SSH 인증 설정 변경의 근거로 사용하지 않는다.

남은 검증은 전체 문서 파싱/이미지 디코딩, 실제 검색 결과, 신규 스캐너와 Supabase 결합, 예약 실행 지속성이다. 단순 읽기 probe는 반복할 필요가 없다. 이 결과로 운영 배포나 대량 임베딩이 승인된 것으로 간주하지 않는다.

## 이전 실패 보고 이력

Cursor 보고에 따르면 Windows Node v24.14.1 프로세스에서 UNC 루트 T/P 및 지정된 PPTX/PNG 샘플이 모두 UNKNOWN으로 실패했고 종료 코드는 1이었다. 탐색 대상 드라이브와 UNC 경로도 현재 세션에서 찾지 못했다. 파일 본문을 읽지 못했으며 운영 스캐너·설정·작업 스케줄러·DB는 변경하지 않았다.

검증 커밋: apollon-nas-scanner `e26e30073159b7e6837ce1567f46935de34880f2`.
보고한 probe SHA-256 `d6e1438de13c736fe4372edd5b061c46c104b1e7eee1133f7111b618ff6fc74b`는 저장소 파일을 CRLF로 체크아웃했을 때의 해시와 정확히 일치한다. 저장소 LF 바이트 해시는 `820faae98a01cd39478a6a9018cd515c94537f7a507d14aee6cbc5fed5508707`. 줄바꿈 차이이며 잘못된 스크립트라는 증거가 아니다.

## 현재 알 수 있는 것

- 이 실행 환경에서 NAS 읽기 검증은 실패했다. 파일 부재, DNS, SMB 연결, 인증, 세션 차이 중 어느 것이 원인인지는 이 결과만으로 확정할 수 없다.
- 과거 03시 예약 스캔 성공과 현재 세션 실패는 서로 다른 시점/실행 문맥의 관측이다. 이를 근거로 현재 NAS 전체 장애나 스캐너 결함을 단정하지 않는다.
- 스크립트가 보고한 UNKNOWN은 정보가 부족했다. 후속 진단 도구는 access/stat/realpath_root/realpath_sample/open/fstat/read/close 단계와 안전한 errno/syscall을 추가한다. 메시지·전체 경로·본문은 출력하지 않는다.

## 다음 접근 검증

같은 실패 환경에서 반복 실행하지 않는다. NAS가 실제로 열리는 회사 Windows 세션에서 먼저 탐색기로 UNC 루트를 확인한다. 탐색기도 실패하면 네트워크/접속 상태 확인이 선행되어야 하며, 코드를 바꾸거나 방화벽/권한/스케줄러를 자동 변경하지 않는다. 탐색기는 열리는데 Cursor 프로세스만 실패하면 동일 사용자 일반 PowerShell에서 읽기 전용 probe를 실행해 결과를 비교한다. 관리자 권한 승격이나 인증정보 복사로 우회하지 않는다.

새 probe 성공도 현재 프로세스의 샘플 1바이트 읽기만 증명한다. 전체 파일 파싱, 매일 03시 작업, 로그오프 실행, 신규 스캐너와 Supabase의 결합 실행은 별도다.

## 두 번째 보고 — 15:29 KST

같은 커밋/CRLF 해시에서 PDF/JPG 샘플도 roots/samples UNKNOWN, 종료 코드 1로 실패했다. 샘플은 Windows Recent에서 선정됐으며 실제 파일의 현재 존재를 확인했다는 뜻은 아니다. 사용자는 aiw/AIW.local의 IP 응답은 확인했지만 net use 재연결이 오류 1223으로 취소됐다고 보고했다. 이 재연결 시도는 Cursor 측 부가 조사이며 ChatGPT가 요청한 읽기 전용 probe의 동작이 아니다.

이 결과는 이름/IP 가시성은 있되 공유 연결/사용자 상호작용 문제가 남아 있음을 시사한다. SMB TCP 445 접근이나 인증 성공까지 확인된 것은 아니다. Windows 오류 1223은 취소를 의미하며 잘못된 암호 또는 Cursor 격리 원인을 단독으로 확정하지 않는다. 같은 PC의 대화형 탐색기에서 UNC를 열어보고, 필요한 인증은 사용자가 Windows에 직접 입력한다. 인증정보를 채팅에 제출하거나 자동 로그인·권한 변경·스케줄러 계정 변경을 하지 않는다.
